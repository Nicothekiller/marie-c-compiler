# Custom tests

Custom c tests for the compiler. All files in this directory should compile properly without errors. 

A nushell script is included in `./compile-tests.nu` to easily compile the tests. You will need to run them through marie manually.

All files in the directory should include a comment to indicate their expected behaviour.
